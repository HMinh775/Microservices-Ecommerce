package com.rainbowforest.apigateway.config;

// File này đã được GỘP VÀO WebSecurityConfig.java
// Giữ lại class rỗng để không thay đổi cấu trúc package,
// nhưng KHÔNG đăng ký thêm bean CORS nào ở đây nữa.
// Tất cả CORS được xử lý trong WebSecurityConfig.

public class CorsConfig {
    // Intentionally empty — CORS is configured in WebSecurityConfig
}